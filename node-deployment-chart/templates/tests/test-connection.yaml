apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-deployment-chart.fullname" . }}-test-connection"
  labels:
    {{- include "node-deployment-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-deployment-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
